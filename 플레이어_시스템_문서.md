# 플레이어 시스템 완전 가이드

## 목차
1. [개요](#개요)
2. [플레이어 이동](#플레이어-이동)
3. [플레이어 체력 시스템](#플레이어-체력-시스템)
4. [공격 시스템](#공격-시스템)
5. [무기 시스템](#무기-시스템)
6. [인챈트 시스템](#인챈트-시스템)
7. [랜덤 능력 시스템](#랜덤-능력-시스템)
8. [테스트 치트키](#테스트-치트키)

---

## 개요

플레이어 시스템은 크게 **5개의 핵심 컴포넌트**로 구성됩니다:

| 컴포넌트 | 역할 | 스크립트 위치 |
|---------|------|---------------|
| **PlayerController** | 이동 처리 | `Assets/Scripts/Controllers/Player/PlayerController.cs` |
| **PlayerStats** | 스탯, 능력, 체력 관리 | `Assets/Scripts/Player/PlayerStats.cs` |
| **AttackManager** | 공격 및 무기 교체 관리 | `Assets/Scripts/Player/Attack/AttackManager.cs` |
| **WeaponBase** | 모든 무기의 기본 클래스 | `Assets/Scripts/Player/Attack/Weapons/WeaponBase.cs` |
| **InputManager** | 입력 처리 (외부) | `Assets/Scripts/Managers/InputManager.cs` |

---

## 플레이어 이동

### 이동 시스템 구조

```
InputManager (마우스/키보드 입력)
    ↓
PlayerController.HandleMove()
    ↓
PlayerController.FixedUpdate()
    ↓
Rigidbody2D.linearVelocity 설정
```

### 주요 코드 (PlayerController.cs)

```csharp
[SerializeField] private float playerSpeed = 3.5f; // 기본 이동속도

void FixedUpdate()
{
    if (movementInput.sqrMagnitude > 0)
    {
        Vector2 moveDirection = movementInput.normalized; // 대각선 가속 방지
        
        // PlayerStats의 이동속도 배율 적용
        float finalSpeed = playerSpeed;
        if (playerStats != null)
        {
            finalSpeed *= playerStats.GetMoveSpeedMultiplier();
        }
        
        rb.linearVelocity = moveDirection * finalSpeed;
    }
}
```

### 이동속도 배율

- **기본 속도**: 3.5
- **질주 능력 1회 획득**: 3.5 × 1.1 = **3.85**
- **질주 능력 2회 획득**: 3.5 × 1.2 = **4.2**

---

## 플레이어 체력 시스템

### 체력 관련 변수 (PlayerStats.cs)

```csharp
[SerializeField] private float playerMaxHP = 100f; // 최대 체력
[SerializeField] private float playerCurHP;        // 현재 체력

public event Action<float> OnHealthChanged;  // 체력 변화 이벤트
public event Action OnPlayerDied;            // 사망 이벤트
```

### 데미지 받기

```csharp
public void TakeDamage(float damage)
{
    // 1. 회피 체크
    if (dodgeChance > 0 && Random.value < dodgeChance)
    {
        Debug.Log("공격 회피!");
        return;
    }

    // 2. 데미지 적용
    playerCurHP -= damage;
    playerCurHP = Mathf.Max(0, playerCurHP);
    OnHealthChanged?.Invoke(playerCurHP);

    // 3. 복수심 발동
    if (hasRevenge && playerCurHP > 0)
    {
        revengeEndTime = Time.time + 5f;
    }

    // 4. 사망 처리
    if (playerCurHP <= 0)
    {
        OnPlayerDied?.Invoke();
        Destroy(gameObject);
    }
}
```

### 몬스터 충돌 시 데미지

```csharp
private void OnTriggerStay2D(Collider2D other)
{
    if (other.CompareTag("Enemy"))
    {
        // 1초마다 한 번씩만 데미지
        if (Time.time - lastDamageTickTime > damageTickCooldown)
        {
            lastDamageTickTime = Time.time;
            float damage = other.GetComponent<MonsterController>()?.normalDamage ?? 10f;
            TakeDamage(damage);
        }
    }
}
```

---

## 공격 시스템

### 자동 공격 시스템 (AttackManager.cs)

```
게임 시작
    ↓
AttackManager.Start()
    ↓
StartAutoAttack() 호출
    ↓
AutoAttackCoroutine() 실행 (매 프레임)
    ↓
currentWeapon.Attack(curLookDir) 호출
```

### 주요 메서드

```csharp
private IEnumerator AutoAttackCoroutine()
{
    while (bIsAutoAttacking)
    {
        if (currentWeapon != null)
        {
            currentWeapon.Attack(curLookDir); // 마우스 방향으로 공격
        }
        yield return null; // 매 프레임 실행
    }
}
```

### 무기 교체 (키보드 입력)

- **1 키**: 검으로 교체
- **2 키**: 창으로 교체
- **3 키**: 활로 교체

```csharp
public void SwitchWeapon(WeaponType weaponType)
{
    // 1. 모든 무기 비활성화
    if (swordObject != null) swordObject.SetActive(false);
    if (spearObject != null) spearObject.SetActive(false);
    if (bowObject != null) bowObject.SetActive(false);

    // 2. 선택한 무기만 활성화
    switch (weaponType)
    {
        case WeaponType.Sword:
            swordObject.SetActive(true);
            currentWeapon = swordObject.GetComponent<SwordWeapon>();
            break;
        // ...
    }
}
```

---

## 무기 시스템

### 무기 계층 구조

```
WeaponBase (추상 클래스)
    ├─ SwordWeapon (검 - 부채꼴 근접 공격)
    ├─ SpearWeapon (창 - 직선 범위 공격)
    └─ BowWeapon (활 - 원거리 투사체)
```

### WeaponBase 공통 기능

```csharp
public abstract class WeaponBase : MonoBehaviour
{
    protected float baseDamage = 10f;          // 기본 데미지
    protected float attackCooldown = 1f;       // 공격 쿨타임
    protected float attackDuration = 0.3f;     // 공격 지속 시간
    public Enchantment curEnchantment;         // 장착된 인챈트

    // 최종 데미지 계산
    public float GetTotalDamage()
    {
        float totalDamage = baseDamage;
        
        // 인챈트 보너스 적용
        if (curEnchantment != null)
        {
            totalDamage += curEnchantment.floatDamageBonus;
        }
        
        // PlayerStats의 공격력 배율 적용
        if (playerStats != null)
        {
            totalDamage *= playerStats.GetAttackDamageMultiplier();
        }
        
        return totalDamage;
    }
    
    // 공격속도 배율 적용된 쿨타임
    public float GetAttackCooldown()
    {
        float cooldown = attackCooldown;
        if (playerStats != null)
        {
            cooldown /= playerStats.GetAttackSpeedMultiplier();
        }
        return cooldown;
    }
}
```

### 검 (SwordWeapon)

#### 특징
- **공격 범위**: 부채꼴 (각도 90도, 반경 1.3)
- **공격 방식**: 범위 내 모든 적 동시 타격

#### 공격 프로세스

```csharp
private IEnumerator PerformSwordAttack()
{
    // 공격 횟수 확인 (2연격 등)
    int attackCount = playerStats.GetAttackCount();
    
    for (int i = 0; i < attackCount; i++)
    {
        // 1. 적 감지
        DetectEnemies(); // 부채꼴 범위 내 적 감지
        
        // 2. 이펙트 생성
        CreateAttackEffect(lastAttackDir);
        
        // 3. 데미지 적용
        foreach (GameObject enemy in enemiesInRange)
        {
            // 인챈트 계산
            int[] appliedEnchants = CalculateAppliedEnchants();
            
            // 데미지 적용
            enemy.GetComponent<MonsterController>()?.TakeDamage(GetTotalDamage());
        }
        
        // 연속 공격 딜레이
        if (i < attackCount - 1)
        {
            yield return new WaitForSeconds(0.1f);
        }
    }
}
```

### 활 (BowWeapon)

#### 특징
- **공격 방식**: 화살 투사체 발사
- **화살 속도**: 15
- **화살 수명**: 3초

#### 공격 프로세스

```csharp
private IEnumerator PerformBowAttack(Vector2 direction)
{
    int attackCount = playerStats.GetAttackCount();
    
    for (int i = 0; i < attackCount; i++)
    {
        // 화살 생성
        GameObject arrowObject = Instantiate(arrowPrefab, attackPoint.position, 
            Quaternion.LookRotation(Vector3.forward, direction));
        
        // 화살에 데미지 전달
        Arrow arrow = arrowObject.GetComponent<Arrow>();
        if (arrow != null)
        {
            arrow.setDamage(GetTotalDamage());
        }
        
        // 연속 발사 딜레이
        if (i < attackCount - 1)
        {
            yield return new WaitForSeconds(0.15f);
        }
    }
}
```

### 창 (SpearWeapon)

#### 특징
- **공격 범위**: 직선 박스 (길이 3, 폭 0.5)
- **공격 방식**: 범위 내 모든 적 동시 타격

---

## 인챈트 시스템

### 인챈트 데이터 구조

```csharp
// Enchantment.cs - ScriptableObject
[CreateAssetMenu(fileName = "Enchantment", menuName = "Scriptable Objects/Enchantment")]
public class Enchantment : ScriptableObject
{
    public string enchantmentName;     // 인챈트 이름
    public float floatDamageBonus;     // 추가 데미지
}
```

### 인챈트 레벨 관리 (PlayerStats.cs)

```csharp
[SerializeField] private int[] enchantmentLevels = new int[4]; // [불, 얼음, 번개, 독]

public int[] GetEnchantmentLevels()
{
    return enchantmentLevels;
}
```

### 인챈트 적용 확률 계산 (SwordWeapon.cs)

```csharp
private int[] CalculateAppliedEnchants()
{
    int[] result = new int[4]; // [불, 얼음, 번개, 독]
    
    int[] enchantLevels = playerStats.GetEnchantmentLevels();
    
    for (int i = 0; i < 4; i++)
    {
        if (enchantLevels[i] > 0)
        {
            // 강화 레벨 * 10% 확률
            float chance = enchantLevels[i] * 10f;
            float randomValue = Random.Range(0f, 100f);
            
            if (randomValue < chance)
            {
                result[i] = enchantLevels[i]; // 성공 시 강화 수치 반환
            }
        }
    }
    
    return result;
}
```

### 인챈트 확률 예시

| 인챈트 레벨 | 적용 확률 |
|------------|----------|
| 1 레벨 | 10% |
| 3 레벨 | 30% |
| 5 레벨 | 50% |
| 10 레벨 | 100% |

### 인챈트 전달 (구현 필요)

```csharp
// 현재 주석 처리됨 - MonsterController에 TakeElement() 구현 필요
// enemy.GetComponent<MonsterController>()?.TakeElement(appliedEnchants);
```

---

## 랜덤 능력 시스템

### 능력 적용 흐름

```
레벨업 발생
    ↓
LevelUpManager가 3개 랜덤 능력 선택
    ↓
플레이어가 1개 선택
    ↓
PlayerStats.SetRandomAbility([능력ID, 레벨])
    ↓
ApplyAbility() 호출
    ↓
스탯 변경 적용
```

### 능력 목록

| ID | 능력명 | 효과 | 치트키 |
|----|--------|------|--------|
| 0 | 2연격 | 공격력 60%, 공격 2회 | Q |
| 1 | 응축된 공격 | 공격속도 30%, 공격력 400% | L |
| 2 | 흡혈 | 적 처치 시 5/10/20/35/70% 확률로 회복 | E |
| 3 | 회피 기동 | 10% 확률로 공격 회피 | R |
| 4 | 질주 | 이동속도 10% 증가 (누적) | T |
| 5 | 빠른 손놀림 | 공격속도 20/40/60/100/200% 증가 | Y |
| 6 | 그림자 은신 | 10/9/7/5/2초마다 0.2초 무적 | U |
| 7 | 분노 | 체력 1% 감소당 공격력 1% 증가 | I |
| 8 | 죽창 | 최대체력 1%, 공격력 1000% | O |
| 9 | 복수심 | 피해 입으면 5초간 공격력 200% | P |

### 능력 코드 예시

#### 2연격 (ID: 0)

```csharp
case 0:
    attackDamageMultiplier *= 0.6f;  // 공격력 60%
    attackCount = 2;                 // 공격 2회
    Debug.Log("2연격 습득! 공격력 60%, 공격 2회");
    break;
```

#### 빠른 손놀림 (ID: 5)

```csharp
case 5:
    float[] attackSpeedBonus = { 0.20f, 0.40f, 0.60f, 1.0f, 2.0f };
    int speedLevel = Mathf.Clamp(level - 1, 0, attackSpeedBonus.Length - 1);
    attackSpeedMultiplier += attackSpeedBonus[speedLevel];
    Debug.Log($"빠른 손놀림 레벨 {level} 습득!");
    break;
```

#### 분노 (ID: 7)

```csharp
case 7:
    hasRage = true;
    Debug.Log("분노 습득! 체력 1% 감소당 공격력 1% 증가");
    break;
```

**분노 적용 계산** (PlayerStats.cs):

```csharp
public float GetAttackDamageMultiplier()
{
    float multiplier = attackDamageMultiplier;

    // 분노 효과
    if (hasRage)
    {
        float healthPercent = playerCurHP / playerMaxHP;
        float rageBuff = (1.0f - healthPercent); // 잃은 체력 비율
        multiplier += rageBuff;
    }

    // 복수심 효과
    if (hasRevenge && Time.time < revengeEndTime)
    {
        multiplier *= 2.0f;
    }

    return multiplier;
}
```

#### 죽창 (ID: 8)

```csharp
case 8:
    playerMaxHP *= 0.01f;                          // 최대체력 1%
    playerCurHP = Mathf.Min(playerCurHP, playerMaxHP);
    attackDamageMultiplier *= 10.0f;              // 공격력 1000%
    OnHealthChanged?.Invoke(playerCurHP);
    Debug.Log("죽창 습득! 최대체력 1%, 공격력 1000%");
    break;
```

---

## 테스트 치트키

### 인챈트 테스트

| 키 | 효과 |
|----|------|
| F1 | 불 인챈트 5레벨 활성화 |
| F2 | 얼음 인챈트 5레벨 활성화 |
| F3 | 번개 인챈트 5레벨 활성화 |
| F4 | 독 인챈트 5레벨 활성화 |

### 랜덤 능력 테스트

| 키 | 능력 |
|----|------|
| Q | 2연격 |
| L | 응축된 공격 |
| E | 흡혈 레벨 1 |
| R | 회피 기동 |
| T | 질주 |
| Y | 빠른 손놀림 레벨 1 |
| U | 그림자 은신 레벨 1 |
| I | 분노 |
| O | 죽창 |
| P | 복수심 |

### 무기 교체

| 키 | 무기 |
|----|------|
| 1 | 검 |
| 2 | 창 |
| 3 | 활 |

---

## Unity Inspector 설정

### Player 오브젝트 (필수)

1. **Tag**: "Player"로 설정
2. **Components**:
   - Rigidbody2D
   - PlayerController
   - PlayerStats
   - AttackManager

### PlayerController

| 필드 | 설정값 |
|------|--------|
| Player Speed | 3.5 |
| Attack Manager | AttackManager 컴포넌트 연결 |

### PlayerStats

| 필드 | 설정값 |
|------|--------|
| Player Max HP | 100 |
| Enchantment Levels | Size: 4, 모두 0 |

### AttackManager

| 필드 | 설정값 |
|------|--------|
| Sword Object | 검 GameObject 연결 |
| Spear Object | 창 GameObject 연결 |
| Bow Object | 활 GameObject 연결 |

### 각 무기 오브젝트 (SwordWeapon, BowWeapon, SpearWeapon)

| 필드 | 설정값 |
|------|--------|
| Base Damage | 10 |
| Attack Cooldown | 1 |
| Attack Duration | 0.3 |
| Attack Point | 공격 시작 위치 Transform |

---

## 데미지 계산 예시

### 기본 공격

```
기본 데미지: 10
→ 최종 데미지: 10
```

### 2연격 + 빠른 손놀림 레벨 3

```
기본 데미지: 10
2연격: × 0.6 = 6
빠른 손놀림: × 1.6 (공격속도만 영향)
→ 최종 데미지: 6 (2회 공격)
→ 공격 쿨타임: 1초 ÷ 1.6 = 0.625초
```

### 죽창 + 분노 (체력 50%)

```
기본 데미지: 10
죽창: × 10 = 100
분노: × 1.5 (체력 50% 손실) = 150
→ 최종 데미지: 150
```

### 응축된 공격 + 복수심 발동

```
기본 데미지: 10
응축된 공격: × 4 = 40
복수심: × 2 = 80
→ 최종 데미지: 80
→ 공격 쿨타임: 1초 ÷ 0.3 = 3.33초
```

---

## 주의사항 및 팁

### 1. Player Tag 필수
- WeaponBase가 `FindGameObjectWithTag("Player")`로 PlayerStats를 찾습니다
- Tag가 없으면 능력이 적용되지 않습니다

### 2. 무기 GameObject 할당
- AttackManager의 Inspector에서 각 무기 GameObject를 반드시 할당해야 합니다
- 할당하지 않으면 무기 교체 시 아무 일도 일어나지 않습니다

### 3. 인챈트 시스템 완성 필요
- 현재 `TakeElement()` 메서드가 MonsterController에 없습니다
- 인챈트 효과를 구현하려면 이 메서드를 추가해야 합니다

### 4. 능력 중복 획득
- 질주는 누적됩니다 (여러 번 획득 가능)
- 2연격은 1번만 의미 있습니다
- 다른 능력들은 레벨 시스템이 있습니다

### 5. 테스트 치트키
- Update()에 구현되어 있으므로 배포 전 제거 필요
- 또는 `#if UNITY_EDITOR` 조건부 컴파일 사용 권장

---

## 확장 가능성

### 향후 추가 가능한 기능

1. **그림자 은신 구현**: 현재 쿨타임만 설정되고 실제 무적 처리는 미구현
2. **흡혈 회복 처리**: `OnEnemyKilled()` 메서드 호출 필요
3. **인챈트 효과 구현**: 화상, 빙결, 감전, 중독 등
4. **능력 조합 시너지**: 2개 이상 능력 조합 시 추가 효과
5. **능력 ScriptableObject화**: 현재는 하드코딩, SO로 분리 가능

---

## 문제 해결 (Troubleshooting)

### 공격이 안 됨
1. AttackManager에 무기 GameObject 할당 확인
2. 무기 GameObject에 WeaponBase 상속 스크립트 부착 확인
3. Player Tag 설정 확인

### 능력이 적용 안 됨
1. Player에 PlayerStats 컴포넌트 확인
2. PlayerController와 WeaponBase에서 playerStats 참조 확인
3. 콘솔에 "습득!" 메시지 나오는지 확인

### 이동속도/공격속도가 안 변함
1. PlayerController/WeaponBase가 GetMoveSpeedMultiplier() 호출하는지 확인
2. PlayerStats 참조가 null이 아닌지 확인

---

**작성일**: 2025년 12월 19일  
**버전**: 1.0  
**작성자**: GitHub Copilot
